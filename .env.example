HOST=0.0.0.0
PORT=3000
# Cleanup interval for expired links/files
CLEANUP_INTERVAL_MINUTES=10
# Admin password for homepage and /admin/* endpoints
ADMIN_PASSWORD=17734
# Admin web session TTL in hours
ADMIN_SESSION_TTL_HOURS=12
# Optional session signing secret (recommended in production)
# ADMIN_SESSION_SECRET=change-me-in-production
